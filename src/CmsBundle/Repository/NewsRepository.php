<?php

namespace CmsBundle\Repository;

/**
 * NewsRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class NewsRepository extends \Doctrine\ORM\EntityRepository
{
    public function search($data) {
        // INICJALIZACJA
        $query = $this->createQueryBuilder('p')
            ->join('p.category', 'g')
            ->where('g.id = :categoryId')
            ->setParameter('categoryId', $data['categoryId']);

        // WYSZUKIWANIE
        if(isset($data['key'])) {
            $query->andWhere('p.title LIKE :key LIKE :key')
                ->setParameter('key', '%'.$data['key'].'%');
        }
        // SORTOWANIE
        if(isset($data['sort'])) {
            $params = explode('.', $data['sort']);

            if (isset($params[1])) {
                $query->orderBy('p.'.$params[0], $params[1]);
            } else {
                $query->orderBy('p.'.$params[0]);
            }

        } else {
            $query->orderBy('p.id', 'ASC');
        }

        return $query->getQuery()->getResult();
    }
}
